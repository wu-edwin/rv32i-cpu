# rtl/

Verilog source files for the RV32I CPU.

Layout:
- `alu.v` — arithmetic logic unit
- `regfile.v` — 32 x 32-bit register file
- `imem.v` — instruction memory
- `dmem.v` — data memory
- `control.v` — control unit
- `pc.v` — program counter
- `cpu.v` — top-level CPU
